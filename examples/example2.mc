// example2.mc — if / else
score = 85;

if (score > 90) {
    grade = 1;
} else {
    grade = 2;
}

print grade;     // prints 2 (since 85 is not > 90)

// Nested math
result = score * 2 - 100;
print result;    // prints 70
